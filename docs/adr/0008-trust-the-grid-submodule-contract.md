# Trust the Grid Submodule Contract

Grid Inspection design and implementation will treat the submodule author's stated behavior as the upstream contract: non-empty `pred_text_L` is a `table_size`-shaped row-major matrix, unrecognized or padded cells contain `"N/G"`, smaller requested dimensions crop the result, and an uninspectable image returns an empty list. The Gateway currently points to submodule commit `b6a5aaa` (tag `2026.08.02`), which keeps the `procd_table`/`procd_table_L` interfaces while refining area cropping and row grouping. API Gateway will not reconstruct or compensate for upstream detection behavior; real-model conformance beyond the deployed smoke test remains unverified rather than blocking the Gateway work.
